#include <stdio.h>

int main() {
    int n,sum=0;
    printf("enter the value of number\n");
    scanf("%d",&n);
    for (int i = 0; i <n; i++)
    {
        sum+=n;
    }
    printf("the summ upto %d numbers is %d",n,sum);
    
printf( "maryadit 125113052\n");
return 0;
}